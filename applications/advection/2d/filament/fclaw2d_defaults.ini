[Options]

     example = 4
     theta = 0
     phi = 0

     mx = 8
     my = 8		# my_leaf

     serialout = T

     subcycle = T         # subcycle in time ( = T or F).

     minlevel = 2         # Minimum level
     maxlevel = 4         # Maximum levels of refinement
     regrid_interval = 1  # Regrid every 'regrid_interval' time steps.

     outstyle = 3         # OutputStyle = 1,2,3 (see below, e.g. [OutputStyle_1]
     tfinal = 8.0           #

     nout = 16000            # Used for all three out styles;  has different meaning, though.
     nstep = 500            # Only used if outstyle is 3

     use_fixed_dt = T     # Take a fixed time step
     initial_dt = 5e-04   # Initial time step for 'minlevel'

     verbosity = 0        # verbose

     ax = 0
     bx = 2
     ay = 0
     by = 2

     meqn = 1             # meqn

     mbc = 2              # mbc
     mthbc = 1 1 1 1      # mthbc(1) (left edge), etc.

#    Leave these as general parameters, since they may be used by several
#    hyperbolic solvers
     max_cfl = 1.0        # maximum cfl
     desired_cfl = 0.9    # desired cfl

     manifold = T         # Manifold

     check_conservation = T
