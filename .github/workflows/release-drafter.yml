name: Release Drafter

on:
  push:
    # branches to consider in the event; optional, defaults to all
    branches:
      - tarball_on_release
  # pull_request event is required only for autolabeler
  pull_request:
    # Only following types are handled by the action, but one can default to all as well
    types: [opened, reopened, synchronize]

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    if: github.repository == 'scottaiton/forestclaw'
    steps:
      # (Optional) GitHub Enterprise requires GHE_HOST variable set
      #- name: Set GHE_HOST
      #  run: |
      #    echo "GHE_HOST=${GITHUB_SERVER_URL##https:\/\/}" >> $GITHUB_ENV

      # Drafts your next Release notes as Pull Requests are merged into "master"
      - name : Create Release Draft
        id: release_draft
        uses: release-drafter/release-drafter@v5
        # (Optional) specify config name to use, relative to .github/. Default: release-drafter.yml
        # with:
        #   config-name: my-config.yml
        #   disable-autolabeler: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          path: forestclaw

      - name: Create tarball
        run: |
          tar -czvf source-code.tar.gz --exclude='.git*' --exclude='.gitignore' forestclaw

      - name: Attach tarball to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload ${{ steps.release_draft.outputs.tag_name }} source-code.tar.gz -R ${{ github.repository }}
